---
description: Instructions for the agent regarding build verification
---

# Build Verification Instruction

The agent should NOT automatically run `npm run build` to verify code changes. 
Building the project should only be done if explicitly requested by the user.
Trust the code generation and linting instead.
